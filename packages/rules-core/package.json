{
  "name": "@sentinel/rules-core",
  "version": "0.1.0",
  "description": "Framework-agnostic AML rules engine with rolling-window state",
  "license": "MPL-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
